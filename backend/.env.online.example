# Online Database Configuration for Frontend/QA Development
# Copy this file to .env and fill in the actual values from your team's secure storage

# PostgreSQL (Neon)
DATABASE_URL=postgresql://${NEON_USER}:${NEON_PASSWORD}@${NEON_HOST}/${NEON_DB}?sslmode=require&channel_binding=require

# MongoDB (Atlas)
MONGODB_URI=mongodb+srv://${ATLAS_USER}:${ATLAS_PASSWORD}@${ATLAS_CLUSTER}/?retryWrites=true&w=majority&appName=${ATLAS_APP_NAME}
MONGO_URL=${MONGODB_URI}

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_ACCESS_SECRET=your-access-secret-key-here
JWT_REFRESH_SECRET=your-refresh-secret-key-here
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# CORS (Update for your frontend URL)
CORS_ORIGIN=http://localhost:5173

# Development Notes:
# 1. For Frontend/QA: Use this with local backend + online DBs
# 2. Get actual connection strings from your team's secure storage
# 3. DO NOT commit real credentials to git
# 4. Contact backend team for access to online DBs