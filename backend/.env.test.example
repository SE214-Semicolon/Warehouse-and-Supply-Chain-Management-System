# E2E Test Environment Configuration
# Copy this file to .env.test and fill in the values

# PostgreSQL Database URL
DATABASE_URL="postgresql://user:password@localhost:5432/warehouse_test?schema=public"

# MongoDB URL
MONGO_URL="mongodb://localhost:27017/warehouse_test"

# JWT Secrets (generate with: node scripts/generate-tokens.js)
JWT_ACCESS_SECRET="your-access-secret-here"
JWT_REFRESH_SECRET="your-refresh-secret-here"

# Logging (keep error for E2E tests)
LOG_LEVEL="error"

# Notes:
# - E2E tests require real databases (PostgreSQL + MongoDB)
# - Start databases with: docker compose up -d db mongodb
# - Run migrations before E2E: npm run migrate:dev
# - Run E2E tests with: npm run test:e2e (currently no tests)
