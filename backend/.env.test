# Test Environment Configuration
NODE_ENV=test
PORT=3000

# Test Database URLs (use same docker DBs but different database name)
DATABASE_URL=postgresql://warehouse_user:warehouse_pass@localhost:5432/warehouse_db
MONGO_URL=mongodb://mongo_user:mongo_pass@localhost:27017/warehouse_db?authSource=admin
MONGODB_DB_NAME=warehouse_analytics

# JWT Configuration for tests
JWT_ACCESS_SECRET=test-access-secret-key-minimum-10-chars
JWT_REFRESH_SECRET=test-refresh-secret-key-minimum-10-chars
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Optional test-specific configs
LOG_LEVEL=error